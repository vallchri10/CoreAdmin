CREATE PROCEDURE [dbo].[Customer_Read]
	@CustomerID NVARCHAR(10)

AS

SET NOCOUNT ON
BEGIN
	SELECT *
	FROM [dbo].Customers
	WHERE CustomerID = @CustomerID
END

IF(@@ROWCOUNT > 0)
	RETURN 1
ELSE
	RETURN 0